const express = require('express');
const router = express.Router();
// ...import necessary middleware and utils...

// Move all /api/search* and autocomplete routes here

module.exports = router;
